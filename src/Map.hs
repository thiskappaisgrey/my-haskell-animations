module Map where

-- what I want to animate is the idea 
mapTS :: Text -- type signature for "List.map" in Ocaml
mapTS = "('a -> 'b) -> 'a List -> 'a List"

-- Think of map as a function that returns another function - rather than a function that takes in an argument
-- given a function: f :: Int -> Int
-- What function do I get out of map?
  
main :: IO ()
main = error "unimplemented"
